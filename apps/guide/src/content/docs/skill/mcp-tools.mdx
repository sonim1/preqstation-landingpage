---
title: MCP Tools
description: PREQSTATION MCP tool reference for the current lifecycle model.
---

The PREQSTATION MCP server exposes read-only tools for discovery and mutation tools for lifecycle work. All mutation tools carry an engine identity.

## Tool Categories

### Read-only tools

| Tool | Purpose |
|---|---|
| `preq_list_projects` | List PREQ projects for setup and local repo mapping |
| `preq_list_tasks` | List tasks by status, label, engine, project key, run state, or dispatch target |
| `preq_get_task` | Fetch one task by ticket or UUID |
| `preq_get_project_settings` | Fetch resolved project settings by project key |

### Mutation tools

| Tool | Purpose |
|---|---|
| `preq_create_task` | Create a new inbox task |
| `preq_plan_task` | Upload a plan and move inbox → todo |
| `preq_start_task` | Claim execution and set `run_state=working` |
| `preq_update_task_note` | Replace task note markdown and attach structured artifacts without changing workflow status |
| `preq_update_task_status` | Manually change workflow status only |
| `preq_update_qa_run` | Update a branch-level QA run with status/report data |
| `preq_complete_task` | Upload execution result and move todo/hold → ready |
| `preq_review_task` | Verify work and move ready → done |
| `preq_block_task` | Upload blocking reason and move active work → hold |
| `preq_delete_task` | Permanently delete a task |

## Engine Resolution

Mutation tools resolve engine identity in this order:

1. Explicit `engine` argument
2. Existing task engine when available
3. MCP client auto-detection from `clientInfo.name`
4. `PREQSTATION_ENGINE`
5. `codex` fallback

Valid engine values:

- `claude-code`
- `codex`
- `gemini-cli`

## Read-only Tools

### preq_list_projects

Parameters: none.

Returns the current PREQ projects that can be used for `/preqstation:setup` and other local mapping flows.

### preq_list_tasks

Parameters:

- `status` (optional): `inbox`, `todo`, `hold`, `ready`, `done`, `archived`
- `label` (optional): label name
- `projectKey` (optional): project key filter
- `engine` (optional): `claude-code`, `codex`, `gemini-cli`
- `runState` (optional): `queued`, `working`
- `dispatchTarget` (optional): `telegram` or `hermes-telegram`
- `limit` (optional): max returned items

Example:

```json
{
  "status": "todo",
  "engine": "claude-code",
  "projectKey": "PROJ",
  "runState": "working",
  "dispatchTarget": "telegram",
  "limit": 20
}
```

### preq_get_task

Parameters:

- `taskId` (required): task key such as `PROJ-123` or a UUID

Returns the full serialized task payload including `status`, `run_state`, `deploy_strategy`, `labels`, `acceptance_criteria`, and structured `artifacts`.

### preq_get_project_settings

Parameters:

- `projectKey` (required): project key such as `PROJ`

Returns:

```json
{
  "project_key": "PROJ",
  "settings": {
    "deploy_strategy": "feature_branch",
    "deploy_default_branch": "main",
    "deploy_auto_pr": true,
    "deploy_commit_on_review": true,
    "deploy_squash_merge": true
  }
}
```

## Mutation Tools

### preq_create_task

Creates a new task in `inbox`.

Parameters:

- `title` (required)
- `repo` (required): repository URL that matches an existing project
- `description` (optional)
- `priority` (optional)
- `labels` (optional)
- `acceptanceCriteria` (optional)
- `branch` (optional)
- `assignee` (optional)
- `engine` (optional)

Example:

```json
{
  "title": "Implement rate limiting",
  "repo": "https://github.com/acme/my-app",
  "engine": "claude-code",
  "priority": "high"
}
```

### preq_plan_task

Uploads plan markdown and moves `inbox` → `todo`.

Parameters:

- `projectKey` (required)
- `taskId` (required)
- `planMarkdown` (required)
- `acceptanceCriteria` (optional)
- `priority` (optional)
- `labels` (optional)
- `engine` (optional)

### preq_start_task

Claims execution before substantive work. This does **not** move the workflow column to `in_progress`; it only marks `run_state=working`.

Parameters:

- `taskId` (required)
- `engine` (optional)

### preq_update_task_status

Manual status-only override.

Parameters:

- `taskId` (required)
- `status` (required): `inbox`, `todo`, `hold`, `ready`, `done`, `archived`
- `engine` (optional)

### preq_update_qa_run

Updates a branch-level QA run without moving the task workflow state.

Parameters:

- `runId` (required)
- `status` (optional): `running`, `passed`, `failed`
- `targetUrl` (optional)
- `reportMarkdown` (optional)
- `artifacts` (optional): structured artifact objects for screenshots, videos, documents, links, local paths, or publication skip reasons
- `summary` (optional): object with `total`, `critical`, `high`, `medium`, and `low`

Keep the markdown report focused on findings. Do not append an `Artifacts:` markdown block; pass artifact metadata through `artifacts`.

### preq_complete_task

Uploads result metadata and moves `todo` or `hold` → `ready`.

Parameters:

- `taskId` (required)
- `summary` (required)
- `tests` (optional)
- `prUrl` (optional)
- `notes` (optional)
- `branchName` (optional)
- `artifacts` (optional): structured artifact objects saved separately from notes and work log markdown
- `engine` (optional)

Example:

```json
{
  "taskId": "PROJ-284",
  "summary": "Implemented rate limiting for login endpoint",
  "engine": "claude-code",
  "branchName": "preqstation/PROJ/rate-limit",
  "prUrl": "https://github.com/org/repo/pull/123",
  "tests": "npm test",
  "artifacts": [
    {
      "type": "image",
      "title": "Desktop screenshot",
      "provider": "fastio",
      "access": "quickshare",
      "expires": "2026-05-10T00:00:00.000Z",
      "url": "https://fast.io/s/example"
    }
  ]
}
```

Artifact object fields are intentionally provider-neutral: `type`, `title`, `url`, `provider`, `access`, `expires`, `localPath`, `reason`, and optional `metadata`. If publication is skipped, include `localPath` and `reason` instead of putting the skip note in an `Artifacts:` markdown block.

### preq_update_task_note

Updates task note markdown without changing workflow status. Ask/prototype flows should use this for note rewrites and pass generated review artifacts separately.

Parameters:

- `taskId` (required)
- `noteMarkdown` (required)
- `artifacts` (optional): structured artifact objects saved separately from task note markdown
- `engine` (optional)

Example:

```json
{
  "taskId": "PROJ-284",
  "noteMarkdown": "## Direction\n\nUse the compact left rail concept.",
  "artifacts": [
    {
      "type": "document",
      "title": "HTML prototype",
      "provider": "fastio",
      "access": "private-workspace",
      "url": "https://fast.io/s/prototype"
    },
    {
      "type": "image",
      "title": "Local screenshot",
      "localPath": "artifacts/prototype.png",
      "reason": "provider unavailable"
    }
  ],
  "engine": "codex"
}
```

### preq_review_task

Moves `ready` → `done` after verification.

Parameters:

- `taskId` (required)
- `summary` (optional): defaults to `"All checks passed"`
- `engine` (optional)

### preq_block_task

Uploads a blocking reason and moves active work to `hold`.

Parameters:

- `taskId` (required)
- `reason` (required)
- `engine` (optional)

### preq_delete_task

Parameters:

- `taskId` (required)

## Branch Handling

When a task already has a `branch`, keep using it as the canonical branch name for deploy steps. `preq_complete_task` accepts `branchName` so agents can send the final branch back to PREQSTATION along with the result.

---
title: System Overview
description: Architecture, components, and data flow of PREQSTATION.
---

import { Aside, Card, CardGrid } from '@astrojs/starlight/components';
import Mermaid from '../../../components/Mermaid.astro';

PREQSTATION is an execution platform for AI-agent-driven development workflows. It bridges developers and coding agents through a task queue, a web app, and agent-facing APIs.

## Three-System Architecture

PREQSTATION consists of three interconnected services:

<CardGrid>
	<Card title="PREQSTATION Web App" icon="setting">
		Central web app. Manages tasks, renders the Kanban UI, exposes the REST API, and can relay Telegram/OpenClaw messages.
	</Card>
	<Card title="preqstation-openclaw" icon="rocket">
		Execution skill package. OpenClaw imports it to resolve projects, create worktrees, and launch coding agents.
	</Card>
	<Card title="preqstation-skill" icon="approve">
		Agent client. MCP server and shell helpers for agents to call the PREQSTATION web app APIs during execution.
	</Card>
</CardGrid>

<Aside>
The public canonical repository is `sonim1/preqstation`. The web app source used for local development is still being migrated there, so setup instructions may reference the current development repository until that move is complete.
</Aside>

## System Diagram

<Mermaid
  chart={`
flowchart LR
  DEV[Developer]
  UI[Kanban UI]
  API[REST API]
  TG[Telegram relay]
  OC[OpenClaw runtime]
  AGENT[Coding agent]
  MCP[preqstation-skill MCP]
  DEV --> UI
  UI --> API
  DEV --> TG
  TG --> OC
  DEV --> OC
  OC --> AGENT
  AGENT --> MCP
  MCP --> API
`}
  caption="OpenClaw is optional. The core system is the web app plus the agent-facing API surface."
/>

## Data Flow

### Task Creation and Notification

```text
User creates task in the Kanban UI
  -> PREQSTATION stores it in PostgreSQL
  -> optional relays notify Telegram / OpenClaw
```

### Agent Trigger via OpenClaw

```text
Developer sends a task request to OpenClaw
  -> OpenClaw resolves project + task + engine
  -> Creates an isolated git worktree
  -> Launches the requested coding agent
```

### Agent Execution and Status Reporting

<Mermaid
  chart={`
flowchart TD
  A[Inbox] --> B[Todo]
  B --> C[Ready]
  C --> D[Done]
  B --> H[Hold]
  H --> C
  Q[Queued overlay] -.-> B
  W[Working overlay] -.-> C
`}
  caption="Workflow status and run state are separate. `queued` and `working` are overlays, not columns."
/>

```text
Agent lifecycle:
  inbox -> preq_plan_task -> todo
  todo or hold -> implement -> preq_complete_task -> ready
  ready -> verify -> preq_review_task -> done
  any blocked step -> preq_block_task -> hold
```

## Tech Stack

| Layer      | Technology                                  |
| ---------- | ------------------------------------------- |
| Framework  | Next.js 16 (App Router)                     |
| UI         | React 19, Mantine 8, Tabler Icons, Recharts |
| ORM        | Drizzle ORM                                 |
| Database   | Neon PostgreSQL (Row Level Security)        |
| Validation | Zod                                         |
| Auth       | Session cookies (HMAC-signed), bcryptjs     |

<Aside>
All data is owner-scoped via Neon PostgreSQL Row Level Security. PREQSTATION is designed for single-developer use.
</Aside>
